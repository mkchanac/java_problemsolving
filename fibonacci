const fib = (n) => {
  if (n <= 2) return 1;
  return fib(n - 1) + fib(n - 2);
};

console.log(fib(3));
console.log(fib(4));
console.log(fib(25));

// javascript code
// remarks: fib function has O(2^n) complexity. 
// fib(25) = taking 2^25 steps... 
